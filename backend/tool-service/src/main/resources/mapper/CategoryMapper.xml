<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.toolrecommend.tool.mapper.CategoryMapper">

    <!-- 查询所有启用的分类 -->
    <select id="selectAllActiveCategories" resultType="com.toolrecommend.common.entity.Category">
        SELECT *
        FROM category
        WHERE is_active = 1
        ORDER BY sort_order DESC, id ASC
    </select>

    <!-- 查询父分类列表 -->
    <select id="selectParentCategories" resultType="com.toolrecommend.common.entity.Category">
        SELECT *
        FROM category
        WHERE parent_id IS NULL
        AND is_active = 1
        ORDER BY sort_order DESC
    </select>

    <!-- 根据父ID查询子分类 -->
    <select id="selectChildCategories" resultType="com.toolrecommend.common.entity.Category">
        SELECT *
        FROM category
        WHERE parent_id = #{parentId}
        AND is_active = 1
        ORDER BY sort_order DESC
    </select>

    <!-- 增加分类工具数量 -->
    <update id="incrementToolCount">
        UPDATE category
        SET tool_count = tool_count + 1
        WHERE id = #{id}
    </update>

    <!-- 减少分类工具数量 -->
    <update id="decrementToolCount">
        UPDATE category
        SET tool_count = tool_count - 1
        WHERE id = #{id}
        AND tool_count > 0
    </update>

</mapper>
