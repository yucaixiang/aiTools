# Tool Recommend 单体应用启动指南

## ⚠️ 重要提示

由于当前命令行环境的Lombok注解处理器配置问题,**必须使用IntelliJ IDEA来编译和启动本项目**。

## 🚀 启动步骤

### 1. 在IntelliJ IDEA中打开项目

```bash
# 项目路径
/Users/bjsttlp324/Desktop/tools/backend/tool-recommend-monolith
```

**方法:**
- 打开IntelliJ IDEA
- File → Open
- 选择上述路径
- 选择"Trust Project"

### 2. 安装Lombok插件(如果还未安装)

1. File → Settings (Mac: IntelliJ IDEA → Preferences)
2. 左侧选择 Plugins
3. 搜索 "Lombok"
4. 点击 Install
5. 重启IDE

### 3. 启用注解处理

1. File → Settings (Mac: IntelliJ IDEA → Preferences)
2. Build, Execution, Deployment → Compiler → Annotation Processors
3. ✅ 勾选 **Enable annotation processing**
4. 点击 Apply → OK

### 4. 等待Maven依赖下载完成

IDE右下角会显示"Importing Maven projects..."或"Indexing..."，等待完成。

### 5. 重新构建项目

**方法一(推荐):**
- Build → Rebuild Project

**方法二:**
- 右键点击 `pom.xml` → Maven → Reload Project
- 然后 Build → Build Project

### 6. 启动应用

**方法一: 使用主类启动**
1. 打开 `src/main/java/com/toolrecommend/ToolRecommendApplication.java`
2. 右键点击文件中的main方法或类名
3. 选择 **Run 'ToolRecommendApplication'**

**方法二: 使用Maven启动**
1. 右侧打开 Maven 面板
2. tool-recommend-monolith → Plugins → spring-boot → spring-boot:run
3. 双击运行

### 7. 验证启动成功

启动成功后,控制台会显示:

```
========================================
   Tool Recommend 单体应用启动成功!
   访问地址: http://localhost:8080
========================================
```

可以访问健康检查接口验证:
```bash
curl http://localhost:8080/actuator/health
```

## 📋 前置条件检查

### ✅ 必须满足以下条件:

#### 1. MySQL数据库
```bash
# 检查MySQL是否运行
mysql -u root -p

# 数据库配置
主机: localhost:3306
数据库: tool_recommend
用户名: root
密码: root123456
```

**如果数据库不存在,需要创建:**
```sql
CREATE DATABASE tool_recommend CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

#### 2. Redis服务
```bash
# 检查Redis是否运行
redis-cli ping
# 应该返回: PONG

# Redis配置
主机: localhost:6379
密码: redis123456
```

**启动Redis(如果未运行):**
```bash
redis-server --requirepass redis123456
```

#### 3. Java环境
```bash
# 检查Java版本(需要Java 17)
java -version
# 应该显示: java version "17.x.x"
```

## 🔧 常见问题

### Q1: 编译时报错"找不到符号 setXXX/getXXX"

**原因:** Lombok注解处理器未启用

**解决:**
1. 确保已安装Lombok插件
2. 启用注解处理 (Settings → Annotation Processors)
3. File → Invalidate Caches / Restart
4. Build → Rebuild Project

### Q2: 启动时报错"Failed to configure a DataSource"

**原因:** MySQL数据库连接失败

**解决:**
1. 检查MySQL服务是否运行
2. 检查数据库`tool_recommend`是否存在
3. 检查用户名密码是否正确(root/root123456)
4. 检查`application.yml`中的数据源配置

### Q3: 启动时报错"Unable to connect to Redis"

**原因:** Redis连接失败

**解决:**
1. 启动Redis服务
2. 检查密码是否为`redis123456`
3. 检查端口是否为6379

### Q4: 端口8080已被占用

**解决方法一: 修改端口**
编辑`application.yml`:
```yaml
server:
  port: 8081  # 改为其他端口
```

**解决方法二: 关闭占用进程**
```bash
# 查找占用8080端口的进程
lsof -i :8080

# 杀死进程
kill -9 <PID>
```

## 🌐 完整启动流程(含Gateway)

### 1. 启动单体应用(本项目)
按照上述步骤在IDE中启动 → 运行在 http://localhost:8080

### 2. 启动Gateway服务
```bash
cd /Users/bjsttlp324/Desktop/tools/backend/gateway-service
mvn spring-boot:run
```
Gateway运行在 http://localhost:8090

### 3. 通过Gateway访问API

所有API请求通过Gateway路由到单体应用:

```bash
# 用户注册
curl -X POST http://localhost:8090/api/users/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","email":"test@example.com","password":"123456"}'

# 用户登录
curl -X POST http://localhost:8090/api/users/login \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"123456"}'

# 查询工具列表
curl http://localhost:8090/api/tools

# 获取工具详情
curl http://localhost:8090/api/tools/1
```

## 📊 项目架构

```
Gateway(8090)
    ↓
Monolith(8080)
    ├─ User Module      (用户管理)
    ├─ Tool Module      (工具管理)
    ├─ Review Module    (评论管理)
    └─ Common Module    (公共组件)
```

## ✅ 已完成的工作

1. ✅ 代码整合 - 所有微服务代码已合并
2. ✅ 配置统一 - pom.xml, application.yml
3. ✅ Bean冲突修复 - 删除重复的配置类
4. ✅ 服务调用重构 - RestTemplate改为本地Service注入
5. ✅ Gateway路由简化 - 统一路由到单体应用
6. ✅ 异常处理统一 - GlobalExceptionHandler

## 📝 待优化项

1. 可以添加AI模块和Admin模块(当前已移除)
2. 可以优化数据库连接池配置
3. 可以添加更多的监控指标
4. 可以配置日志文件输出

## 💡 提示

- 首次启动可能需要较长时间(下载依赖、初始化数据库连接等)
- 建议使用IDEA的Debug模式启动,方便调试
- 如果修改了代码,需要重新编译(Build → Build Project)
- 数据库表结构会由MyBatis Plus自动创建(如果启用了ddl-auto)

## 📞 需要帮助?

如果遇到问题:
1. 检查IDEA的Build输出,查看编译错误
2. 检查Run输出,查看启动日志
3. 检查数据库和Redis连接
4. 确认Lombok插件已正确安装和配置
