# 前端快速启动指南

## 🚀 快速启动新版Web前端

### 方式一：直接启动（推荐）

```bash
cd /Users/bjsttlp324/Desktop/tools/frontend/web
npm run dev
```

访问: **http://localhost:3001**

### 方式二：从项目根目录启动

```bash
cd /Users/bjsttlp324/Desktop/tools
cd frontend/web && npm run dev
```

## 📦 如果需要重新安装依赖

```bash
cd /Users/bjsttlp324/Desktop/tools/frontend/web
rm -rf node_modules package-lock.json
npm install
npm run dev
```

## 🎯 前端项目对比

### 新版PC Web前端 ⭐ 推荐

**位置**: `frontend/web/`
**端口**: 3001
**特点**:
- ✅ PC端优先设计
- ✅ 现代化UI
- ✅ 整洁美观
- ✅ 首页合并发现页
- ✅ 响应式布局

**启动命令**:
```bash
cd frontend/web
npm run dev
```

### 管理后台

**位置**: `frontend/admin/`
**端口**: 3001 (默认)
**特点**:
- ✅ 后台管理功能
- ✅ Element Plus UI
- ✅ 数据统计和可视化

**启动命令**:
```bash
cd frontend/admin
npm run dev
```

### UniApp版本（旧版）

**位置**: `frontend/uniapp/`
**端口**: 3000
**特点**:
- 移动端风格
- 不推荐用于PC端

**启动命令**:
```bash
cd frontend/uniapp
npm run dev:h5
```

## ⚠️ 端口冲突处理

如果端口被占用，Vite会自动切换到下一个可用端口：
- 默认: 3000
- 备用: 3001, 3002, 3003...

查看实际端口请看启动日志：
```
VITE v5.4.21  ready in 596 ms

➜  Local:   http://localhost:3001/
```

## 🔄 同时运行多个前端

### 场景1: Web前端 + 管理后台

```bash
# 终端1: Web前端
cd frontend/web
npm run dev

# 终端2: 管理后台
cd frontend/admin
npm run dev
```

它们会自动使用不同端口。

### 场景2: 仅Web前端

```bash
cd frontend/web
npm run dev
```

## 📝 开发提示

### 修改端口

编辑 `frontend/web/vite.config.js`:

```javascript
export default defineConfig({
  server: {
    port: 3000,  // 改为你想要的端口
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

### API代理配置

所有 `/api` 开头的请求会被代理到 `http://localhost:8080`

确保后端网关服务已启动！

### 热更新

修改代码后会自动刷新浏览器，无需手动刷新。

## 🏗️ 构建生产版本

```bash
cd frontend/web
npm run build
```

构建产物在 `dist/` 目录。

预览构建结果:
```bash
npm run preview
```

## 📂 项目结构

```
frontend/web/
├── src/
│   ├── views/          # 页面
│   │   ├── Home.vue    # 首页（合并了发现页）
│   │   ├── ToolDetail.vue
│   │   ├── Chat.vue
│   │   ├── Profile.vue
│   │   └── Login.vue
│   ├── api/            # API接口
│   ├── stores/         # 状态管理
│   └── router/         # 路由配置
└── package.json
```

## 🎨 页面访问

访问 http://localhost:3001 后：

- **首页**: `/` - 工具浏览、搜索、筛选
- **工具详情**: `/tool/:id` - 点击工具卡片进入
- **AI助手**: `/chat` - 顶部导航进入
- **个人中心**: `/profile` - 需要登录
- **登录**: `/login` - 点击登录按钮

## ❓ 常见问题

### Q: 页面空白？
A: 检查浏览器控制台错误，可能是API连接问题。

### Q: API请求失败？
A: 确保后端服务已启动在8080端口。

### Q: 端口被占用？
A: Vite会自动切换端口，查看启动日志获取实际端口。

### Q: 修改代码不生效？
A: 检查浏览器是否禁用了热更新，或手动刷新。

## 🔗 相关文档

- `前端改版完成说明.md` - 详细改版说明
- `frontend/web/README.md` - 项目README
- `frontend/web/前端重构说明.md` - 技术重构文档

## ✅ 快速检查清单

启动前确认:
- [ ] Node.js已安装（v16+）
- [ ] 在正确的目录 (`frontend/web/`)
- [ ] 依赖已安装 (`npm install`)
- [ ] 后端服务已启动（可选）

启动后确认:
- [ ] 看到 "ready in" 提示
- [ ] 浏览器能打开对应地址
- [ ] 首页正常显示

---

**一键启动命令**:
```bash
cd /Users/bjsttlp324/Desktop/tools/frontend/web && npm run dev
```

🎉 **开始使用吧！**
