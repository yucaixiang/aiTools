# AI工具推荐系统 - 快速开始指南

> 🚀 从零到部署,30分钟快速启动完整系统

---

## 📋 前置要求

在开始之前,请确保您的环境满足以下要求:

### 必需软件

| 软件 | 版本要求 | 下载地址 | 用途 |
|-----|---------|---------|------|
| **Docker** | 20.10+ | https://www.docker.com/ | 容器化部署 |
| **Maven** | 3.8+ | https://maven.apache.org/ | Java项目构建 |
| **JDK** | 17+ | https://adoptium.net/ | Java运行环境 |
| **Node.js** | 18+ | https://nodejs.org/ | 前端开发 |
| **Git** | 2.0+ | https://git-scm.com/ | 版本控制 |

### 系统要求

- **操作系统**: macOS / Linux / Windows (推荐macOS或Linux)
- **内存**: 至少 8GB (推荐 16GB)
- **硬盘**: 至少 20GB 可用空间
- **CPU**: 4核心+ (AI模型需要较高算力)

### 端口检查

确保以下端口未被占用:
- **3306** - MySQL
- **6379** - Redis
- **9200** - Elasticsearch
- **5601** - Kibana
- **6333** - Qdrant
- **11434** - Ollama
- **8080** - Gateway Service
- **8081** - Tool Service
- **8082** - User Service
- **8083** - AI Service
- **8084** - Review Service
- **8085** - Admin Service

检查端口占用:
```bash
# macOS/Linux
lsof -i:3306

# 如果端口被占用,可以停止占用进程或修改配置文件中的端口
```

---

## 🚀 快速开始 (推荐)

### 方式一: 一键部署 ⭐

最简单的方式,使用自动化脚本:

```bash
# 1. 进入项目目录
cd /Users/bjsttlp324/Desktop/tools

# 2. 给脚本添加执行权限 (如果还没有)
chmod +x deploy.sh

# 3. 运行一键部署脚本
./deploy.sh
```

**脚本会自动完成**:
1. ✅ 检查Docker/Maven/Node环境
2. ✅ 启动Docker基础设施 (MySQL/Redis/ES/Ollama)
3. ✅ 初始化数据库 (建表+示例数据)
4. ✅ 编译后端服务 (Maven打包)
5. ⚠️ 提示手动启动后端服务 (需要IntelliJ IDEA或命令行)
6. ✅ 显示访问信息

**预计时间**: 30-45分钟 (首次需下载Docker镜像)

---

## 📝 分步部署 (手动控制)

如果您想完全理解和控制部署过程,按以下步骤操作:

### 步骤 1: 启动基础设施

```bash
# 进入部署目录
cd deployment

# 启动所有基础服务
docker-compose up -d

# 查看服务状态
docker-compose ps

# 预计耗时: 10-30分钟 (首次需下载镜像)
```

**启动的服务**:
- MySQL 8.0 (端口3306)
- Redis 7 (端口6379)
- Elasticsearch 8.11 (端口9200)
- Kibana 8.11 (端口5601)
- Qdrant (端口6333)
- Ollama (端口11434)

**等待MySQL就绪**:
```bash
# 检查MySQL是否启动成功
docker exec tool-recommend-mysql mysqladmin ping -h localhost -uroot -proot123456

# 出现 "mysqld is alive" 表示成功
```

### 步骤 2: 初始化数据库

```bash
# 回到项目根目录
cd ..

# 执行建表脚本
docker exec -i tool-recommend-mysql mysql -uroot -proot123456 < scripts/init-db.sql

# 导入示例数据
docker exec -i tool-recommend-mysql mysql -uroot -proot123456 tool_recommend < scripts/init-sample-data.sql

# 验证数据
docker exec -it tool-recommend-mysql mysql -uroot -proot123456 -e "USE tool_recommend; SELECT COUNT(*) FROM ai_tools;"

# 应该看到30+条工具数据
```

### 步骤 3: 编译后端服务

```bash
# 进入后端目录
cd backend

# 清理旧构建
mvn clean

# 编译打包 (跳过测试,加快速度)
mvn package -DskipTests

# 预计耗时: 3-5分钟
```

**编译成功标志**:
```
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
[INFO] Total time: XX:XX min
```

### 步骤 4: 启动后端服务

有两种启动方式可选:

#### 方式A: IntelliJ IDEA (推荐) ⭐

1. **打开项目**
   - File → Open → 选择 `backend` 文件夹
   - 等待Maven导入完成 (首次较慢)

2. **配置JDK**
   - File → Project Structure → Project SDK
   - 选择JDK 17或更高版本

3. **启动服务** (按以下顺序)
   - 找到每个服务的 `*Application.java` 文件
   - 右键 → Run

   **启动顺序**:
   1. `tool-service/src/main/java/.../ToolServiceApplication.java`
   2. `user-service/src/main/java/.../UserServiceApplication.java`
   3. `ai-service/src/main/java/.../AiServiceApplication.java`
   4. `review-service/src/main/java/.../ReviewServiceApplication.java`
   5. `admin-service/src/main/java/.../AdminServiceApplication.java`
   6. `gateway-service/src/main/java/.../GatewayServiceApplication.java` (最后启动)

4. **验证启动**
   - 每个服务启动后,在控制台看到:
     ```
     Started *ServiceApplication in X.XXX seconds
     ```

#### 方式B: 命令行

在**不同的终端窗口**中运行 (或使用后台运行):

```bash
# Terminal 1 - Tool Service
cd tool-service
mvn spring-boot:run

# Terminal 2 - User Service
cd user-service
mvn spring-boot:run

# Terminal 3 - AI Service
cd ai-service
mvn spring-boot:run

# Terminal 4 - Review Service
cd review-service
mvn spring-boot:run

# Terminal 5 - Admin Service
cd admin-service
mvn spring-boot:run

# Terminal 6 - Gateway (最后启动)
cd gateway-service
mvn spring-boot:run
```

**后台运行方式** (推荐):
```bash
cd tool-service
nohup mvn spring-boot:run > logs/tool-service.log 2>&1 &

cd ../user-service
nohup mvn spring-boot:run > logs/user-service.log 2>&1 &

# ... 依此类推
```

**查看日志**:
```bash
tail -f tool-service/logs/tool-service.log
```

### 步骤 5: 拉取AI模型 (首次必须)

```bash
# 进入Ollama容器
docker exec -it tool-recommend-ollama bash

# 拉取Qwen2.5:7b模型 (约7GB,需要一些时间)
ollama pull qwen2.5:7b

# 验证模型
ollama list

# 测试模型
ollama run qwen2.5:7b "你好,介绍一下自己"

# 退出容器
exit
```

### 步骤 6: 验证系统

#### 健康检查

```bash
# 检查Gateway
curl http://localhost:8080/actuator/health
# 预期: {"status":"UP"}

# 检查Tool Service
curl http://localhost:8081/actuator/health

# 检查User Service
curl http://localhost:8082/actuator/health

# 检查AI Service
curl http://localhost:8083/actuator/health

# 检查Review Service
curl http://localhost:8084/actuator/health

# 检查Admin Service
curl http://localhost:8085/actuator/health
```

#### API测试

**方式A: 使用测试脚本** (推荐)
```bash
# 回到项目根目录
cd /Users/bjsttlp324/Desktop/tools

# 运行测试脚本
./test-api.sh

# 预期: 27个测试全部通过
```

**方式B: 手动测试**
```bash
# 1. 测试用户注册
curl -X POST http://localhost:8080/api/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "email": "test@example.com",
    "password": "Test123456",
    "nickname": "测试用户"
  }'

# 2. 测试用户登录
curl -X POST http://localhost:8080/api/users/login \
  -H "Content-Type: application/json" \
  -d '{
    "account": "testuser",
    "password": "Test123456"
  }'

# 3. 测试查询热门工具
curl http://localhost:8080/api/tools/hot?limit=5

# 4. 测试AI推荐
curl "http://localhost:8080/api/ai/recommend?query=设计工具&limit=5"
```

---

## 🎯 前端开发

### 开发模式

```bash
# 进入前端目录
cd frontend/uniapp

# 安装依赖 (首次)
npm install

# 启动H5开发服务器
npm run dev:h5

# 访问: http://localhost:3000
```

### 生产构建

```bash
# H5平台
npm run build:h5

# 小程序平台
npm run build:mp-weixin

# APP平台
npm run build:app
```

---

## 🔍 访问系统

### 后端服务

| 服务名称 | 端口 | 健康检查 | 说明 |
|---------|-----|---------|------|
| **Gateway** | 8080 | http://localhost:8080/actuator/health | API网关 |
| **Tool Service** | 8081 | http://localhost:8081/actuator/health | 工具服务 |
| **User Service** | 8082 | http://localhost:8082/actuator/health | 用户服务 |
| **AI Service** | 8083 | http://localhost:8083/actuator/health | AI服务 |
| **Review Service** | 8084 | http://localhost:8084/actuator/health | 评论服务 |
| **Admin Service** | 8085 | http://localhost:8085/actuator/health | 管理服务 |

**主要API入口**:
```
http://localhost:8080/api/
```

### 前端应用

| 平台 | 访问方式 | 说明 |
|-----|---------|------|
| **H5开发** | http://localhost:3000 | 浏览器访问 |
| **H5生产** | nginx部署 | 需配置Web服务器 |
| **微信小程序** | 微信开发者工具 | 导入项目 |

### 基础设施

| 服务 | 访问方式 | 凭证 | 说明 |
|-----|---------|------|------|
| **MySQL** | localhost:3306 | root / root123456 | 数据库 |
| **Redis** | localhost:6379 | 无密码 | 缓存 |
| **Elasticsearch** | http://localhost:9200 | 无 | 搜索引擎 |
| **Kibana** | http://localhost:5601 | 无 | ES可视化 |
| **Ollama** | http://localhost:11434 | 无 | AI模型 |
| **Qdrant** | http://localhost:6333 | 无 | 向量数据库 |

---

## 🛠️ 常见问题

### 1. Docker镜像下载太慢

**解决方案**: 配置国内镜像源

编辑 Docker配置文件 (macOS: `~/.docker/daemon.json`):
```json
{
  "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://huecker.io",
    "https://dockerhub.timeweb.cloud"
  ]
}
```

重启Docker Desktop。

### 2. MySQL容器启动失败

**可能原因**:
- 端口3306被占用
- 数据卷权限问题

**解决方案**:
```bash
# 检查端口占用
lsof -i:3306

# 停止占用进程或修改docker-compose.yml端口:
ports:
  - "3307:3306"

# 清理并重新创建
docker-compose down -v
docker-compose up -d mysql
```

### 3. 后端服务启动失败

**可能原因**:
- 数据库连接失败
- 端口冲突
- 依赖未正确安装

**排查步骤**:
```bash
# 1. 检查MySQL是否运行
docker ps | grep mysql

# 2. 测试数据库连接
mysql -h127.0.0.1 -uroot -proot123456

# 3. 检查端口占用
lsof -i:8081

# 4. 查看完整日志
cd backend/tool-service
mvn spring-boot:run
# 观察控制台输出
```

### 4. AI服务无响应或超时

**可能原因**:
- Ollama未启动
- 模型未下载
- 首次调用需加载模型 (10-15秒)

**解决方案**:
```bash
# 检查Ollama状态
docker ps | grep ollama

# 检查模型
docker exec tool-recommend-ollama ollama list

# 拉取模型 (如果没有)
docker exec tool-recommend-ollama ollama pull qwen2.5:7b

# 预热模型 (可选)
curl http://localhost:11434/api/generate -d '{
  "model": "qwen2.5:7b",
  "prompt": "你好"
}'
```

### 5. 前端API调用失败

**可能原因**:
- 后端服务未启动
- CORS配置问题
- Token过期

**解决方案**:
```bash
# 1. 检查Gateway是否运行
curl http://localhost:8080/actuator/health

# 2. 检查浏览器控制台
# 打开开发者工具 → Network标签
# 查看失败的请求详情

# 3. 清除本地存储
localStorage.clear()
# 然后重新登录
```

### 6. Maven编译失败

**可能原因**:
- 网络问题,依赖下载失败
- Maven配置问题

**解决方案**:
```bash
# 配置Maven国内镜像 (~/.m2/settings.xml)
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>*</mirrorOf>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>

# 清理并重新编译
mvn clean install -U -DskipTests
```

---

## 📚 下一步

### 学习资料

1. **项目文档** (推荐按此顺序阅读)
   - `README_FINAL.md` - 项目总览
   - `系统架构设计.md` - 架构设计
   - `API接口文档.md` - API详细说明
   - `数据库设计文档.md` - 数据库设计
   - `前端开发文档.md` - 前端指南

2. **开发指南**
   - `开发规范.md` - 编码规范
   - `Git工作流程.md` - 版本管理
   - `部署运维指南.md` - 部署详解

3. **测试与优化**
   - `测试报告.md` - 测试结果
   - `性能优化建议.md` - 优化方案
   - `问题排查手册.md` - 故障排查

### 功能扩展

**短期扩展** (1-2周):
- [ ] 完成Elasticsearch全文搜索集成
- [ ] 实现Qdrant向量搜索
- [ ] 完善前端收藏页API集成
- [ ] 性能优化 (慢查询、缓存策略)

**中期扩展** (1-2月):
- [ ] 部署Prometheus + Grafana监控
- [ ] CI/CD pipeline (Jenkins/GitLab CI)
- [ ] 用户行为分析
- [ ] 个性化推荐增强

**长期扩展** (3-6月):
- [ ] 服务集群化部署
- [ ] 数据库主从复制
- [ ] 社交功能 (关注、分享)
- [ ] 国际化 (多语言)
- [ ] 移动APP (iOS/Android)

### 贡献代码

1. Fork项目
2. 创建特性分支: `git checkout -b feature/AmazingFeature`
3. 提交更改: `git commit -m 'Add some AmazingFeature'`
4. 推送分支: `git push origin feature/AmazingFeature`
5. 提交Pull Request

**编码规范**: 参考 `开发规范.md`

---

## 🆘 获取帮助

### 文档资源

- **项目文档**: `/Users/bjsttlp324/Desktop/tools/docs/`
- **在线文档**: (如果部署)
- **API文档**: http://localhost:8080/doc.html (如果配置Swagger)

### 日志查看

```bash
# Docker容器日志
docker logs tool-recommend-mysql
docker logs tool-recommend-redis
docker logs tool-recommend-elasticsearch
docker logs tool-recommend-ollama

# 后端服务日志
tail -f backend/tool-service/logs/spring.log
tail -f backend/user-service/logs/spring.log
# ... 其他服务

# 前端开发日志
# 浏览器开发者工具 → Console
```

### 数据库访问

```bash
# 连接MySQL
docker exec -it tool-recommend-mysql mysql -uroot -proot123456

# 使用数据库
USE tool_recommend;

# 查看表
SHOW TABLES;

# 查询数据
SELECT * FROM ai_tools LIMIT 10;
```

### Redis访问

```bash
# 连接Redis
docker exec -it tool-recommend-redis redis-cli

# 查看所有键
KEYS *

# 查看键值
GET tool:hot:list

# 退出
quit
```

---

## ✅ 验收清单

部署完成后,请确认以下项目:

### 基础设施 ✅
- [ ] MySQL容器运行正常
- [ ] Redis容器运行正常
- [ ] Elasticsearch容器运行正常
- [ ] Ollama容器运行正常
- [ ] Qwen2.5:7b模型已下载

### 后端服务 ✅
- [ ] Tool Service (8081) 启动成功
- [ ] User Service (8082) 启动成功
- [ ] AI Service (8083) 启动成功
- [ ] Review Service (8084) 启动成功
- [ ] Admin Service (8085) 启动成功
- [ ] Gateway Service (8080) 启动成功

### 数据库 ✅
- [ ] tool_recommend数据库创建成功
- [ ] 14张数据表创建成功
- [ ] 示例数据导入成功 (30+工具)

### API测试 ✅
- [ ] 用户注册接口正常
- [ ] 用户登录接口正常
- [ ] 工具查询接口正常
- [ ] AI推荐接口正常
- [ ] 评论接口正常
- [ ] 测试脚本27/27通过

### 前端应用 (可选) ✅
- [ ] H5应用编译成功
- [ ] 开发服务器启动成功
- [ ] 页面正常访问
- [ ] API调用正常

---

## 🎉 恭喜!

如果您完成了以上所有步骤,那么恭喜您已经成功部署了AI工具推荐系统!

**接下来您可以**:
- 🔍 浏览30+个AI工具
- 💬 与AI对话,获取工具推荐
- ⭐ 点赞和收藏喜欢的工具
- ✍️ 撰写工具评论
- 🛠️ 二次开发,添加新功能
- 📚 深入学习项目代码

**祝您使用愉快! 🎊**

---

## 📞 联系方式

- **项目地址**: `/Users/bjsttlp324/Desktop/tools/`
- **文档目录**: `/Users/bjsttlp324/Desktop/tools/docs/`
- **Issue追踪**: (GitHub Issues,如果开源)

**技术栈**:
- 后端: Spring Boot 3 + Spring Cloud + MyBatis Plus + Redis
- 前端: Vue 3 + TypeScript + UniApp
- AI: Ollama + Qwen2.5:7b
- 数据库: MySQL 8 + Elasticsearch + Qdrant
- 部署: Docker + Docker Compose

**版本**: v1.0.0
**完成度**: 95%
**生产就绪**: ⭐⭐⭐⭐ (4/5)
